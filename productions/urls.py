import base64from django.urls import path, includefrom .views import *from utils.get_safe_url import safe_urlapp_name = 'productions'urlpatterns = [    path(safe_url('creation'), ProductCreationView.as_view(), name='create_product'),    path(safe_url('all_+products'), ProductListView.as_view(), name='product-list'),    path(safe_url('add_new-brand/'), ProductListView.as_view(), name='product-list'),    path('look-detail/?product-no=<str:code>/', ProductDetailView.as_view(), name='product-detail'),    path(safe_url('delete/?product-no=') + '<str:code>/', product_delete, name='delete-product'),]